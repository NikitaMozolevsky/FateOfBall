1) Создать 12 площадок по которым будет катится шарик. *
2) Создать шар, привязать к нем камеру, задать движение шару вдоль оси Z. *

4) Мне нужно что бы на свем проекте была только одна переменная скорости и поворота,
 которая должна быть показана в инспекторе. *
5) Нужно реализовать выход шара за пределы и в результате его падение. Можно отключать скрипт дви-
жения, тогда будет действовать сила притяжения. (Сделано изменением позиции). *
6) Шар должен падать на платформу с верху. Камера должна привязываться к шару в момент касания его
первой платформы. Кнопка play должна выскакивать с низу после касания шара платформой *
(с эффектом подпрыгивания) -.
7) Задать функциональность кнопке паузы. *
8) Изменять цвет фона во время игры. ***
    * При запуске если метод LoadColor возвращает null нужно создать новое значение 
    полученное от текущенго значения backgroundColor и засунуть в приватную переменную 
    colorData, если LoadColor не возврашает null, то преобразовать полученное значение в 
    ColorData и засунуть в приватную переменную colorData, если игра началась менять значения
    переменной colorData (возможно нужно сделать публичной т.к. часто будет исользоваться
    сеттер) и после изменения присваивать значения rgb backgroundColor, в методе который 
    будет завершать игру сохранять в PlayerPrefs крайнее значение ColorData.
9) Плавно пееремещать кнопки. *
10) Восстановление игры после паузы. *

3 -> ) Сделать генерацию платформ до 100 в easy +, middle- и hard- стиле.
    * Плтформа должна подниматься и опускаться. +
    * Создать метод который зпаскается при нажатии кнопки restart, он очищает очередь и
     удаляет объекты находящиеся в очереди, засовывает в очередь
    2 платформы (создать метод), далее начинается цикл while, который работает пока нет состояния
    lose, внутри этого метода +
    * Первые 2 элемента очереди стандартные
    * Добавить в очередь еще 98 платформ в рандомных направлениях.
    * На сцене изначатьно не видно платформы.
    * Повесить скрипт на GameObject с платформами.
11) Если шар падает на -5: 
    * Фиксировать камеру на позиции где зафиксировано падение по Y 2.5 
    (либо фиксировать на отметке -5). *
    * Прекратить изменение цвета. *
    * После выхода шара за пределы видимости включить панель паузы -
    (изменить название панели на stopGame).
    * isPlay - false. *
    * Выдвигать Image с результатом. -
12) Рестарт: *
    * Создать кнопку Restart. *
    * Создать фиксированную FreeLockCamera на которую mainCamera возвращается при нажатии Restart. *
 